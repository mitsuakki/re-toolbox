{
  "_comment": "Snippet to merge into your MCP client config (e.g. claude_desktop_config.json).",
  "mcpServers": {
    "radare2": {
      "command": "docker",
      "args": ["exec", "-i", "ctf-re", "r2pm", "-r", "r2mcp"]
    },
    "ghidra-headless": {
      "_comment": "Talks to the headless ghidra-mcp HTTP server started by entrypoint.sh on :8089",
      "command": "docker",
      "args": [
        "exec", "-i", "ctf-re",
        "python3", "/opt/tools/ghidra-mcp/bridge_mcp_ghidra.py",
        "--ghidra-server", "http://127.0.0.1:8089/"
      ]
    },
    "ghidra-gui": {
      "_comment": "Connect to Ghidra GUI running on your host with the GhidraMCP plugin started (Tools > GhidraMCP > Start MCP Server, default port 8080)",
      "command": "docker",
      "args": [
        "exec", "-i", "ctf-re",
        "python3", "/opt/tools/ghidra-mcp/bridge_mcp_ghidra.py",
        "--ghidra-server", "http://host.docker.internal:8080/"
      ]
    },
    "shell": {
      "_comment": "Generic shell command execution inside the container — gives access to all CLI tools",
      "command": "docker",
      "args": [
        "exec", "-i", "ctf-re",
        "python3", "/opt/tools/scripts/mcp/shell-mcp.py"
      ]
    },
    "python": {
      "_comment": "Python REPL with angr, pwntools, z3, lief, capstone pre-loaded",
      "command": "docker",
      "args": [
        "exec", "-i", "ctf-re",
        "python3", "/opt/tools/scripts/mcp/python-mcp.py"
      ]
    },
    "fuzz": {
      "_comment": "Fuzzing harness — scaffold AFL++ or honggfuzz sessions, run smoke tests, triage crashes",
      "command": "docker",
      "args": [
        "exec", "-i", "ctf-re",
        "python3", "/opt/tools/scripts/mcp/fuzz-mcp.py"
      ]
    },
    "angr": {
      "_comment": "Symbolic analysis — CFG overview, symbolic path finding, function listing",
      "command": "docker",
      "args": [
        "exec", "-i", "ctf-re",
        "python3", "/opt/tools/scripts/mcp/angr-mcp.py"
      ]
    }
  }
}
